[project]
name = "m1-question-pipeline"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["Pillow>=10.0", "tqdm>=4.66"]

[project.scripts]
m1-question-build = "m1_curriculum_mapper.build:main"

[project.optional-dependencies]
step-fill = ["openai>=1.0"]
