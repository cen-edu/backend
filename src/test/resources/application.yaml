spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:cen_edu;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
