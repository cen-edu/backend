/**
 * 문제 생성·수정·검증·채점 경로가 함께 사용하는 문항 스냅샷 계약을 정의한다.
 *
 * <p>이 패키지의 타입은 JPA 엔티티나 HTTP DTO가 아니다. 아직 저장되지 않은 문항도 표현할 수
 * 있도록 DB ID, 저장소 URL, 검증 상태와 같은 실행·영속화 정보를 포함하지 않는다. 문자열 키는
 * 하나의 수정 세션에서 버전이 바뀌어도 같은 구성 요소를 식별하는 논리 키다.
 *
 * <p>필드의 조합과 참조 무결성은 별도 Validator가 담당한다. 이 패키지의 record는 Spring AI의
 * 구조화 출력과 버전 스냅샷 직렬화가 공유하는 순수 데이터 계약만 제공한다.
 */
package com.cenedu.backend.domain.problem.authoring.model;
