package com.cenedu.backend.domain.problem.authoring.diagram;

public record DiagramStyle(String strokeColor, String fillColor, String accentColor, int strokeWidth, String fontFamily,
                           int fontSize) {
}
