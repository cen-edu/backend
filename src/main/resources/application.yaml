spring:
  application:
    name: cen-edu
  profiles:
    default: local
  config:
    # 프로젝트 루트의 .env 를 프로퍼티 파일로 읽는다. 파일이 없어도 기동된다(optional).
    # 실제 OS 환경 변수가 이 파일보다 우선순위가 높다.
    import: optional:file:.env[.properties]
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/cen_edu}
    username: ${DB_USERNAME:cen}
    password: ${DB_PASSWORD:cen_local_password}
  jpa:
    hibernate:
      ddl-auto: update          # 3단계에서 validate 로 고정 전환
    open-in-view: false
  flyway:
    enabled: false              # 3단계에서 V1 baseline 과 함께 true
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
