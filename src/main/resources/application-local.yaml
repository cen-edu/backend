spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.orm.jdbc.bind: trace

app:
  cors:
    allowed-origins: http://localhost:5173   # Vite 기본 포트. 5단계 CorsConfig 에서 소비한다.
